/**
 * Templates Hub
 */

// Dependencies
const welcome = require('./welcome');
const verify = require('./verify');
const resetPassword = require('./resetPassword');

module.exports = {
    welcome,
    verify,
    resetPassword,
}