'use strict';

const defaultRoutes = {
	index: '/',
};

const userRoutes = {};

const adminRoutes = {
	create: '/api/admin/create',
	login: 'api/admin/login',
};

const patientRoutes = {};

module.exports = {
	defaultRoutes,
	userRoutes,
	adminRoutes,
	patientRoutes,
};
